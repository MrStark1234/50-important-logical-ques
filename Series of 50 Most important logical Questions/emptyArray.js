//How to Empty an Array in JavaScript
//Do not reset it to an New Array and do not run any loop to pop the elements
//And also don't write any function.

let arr = [1, 2, 3, 4];
console.log(arr);
arr.length = 0;
console.log(arr);
